---
interface Props {
    title: string
    description: string
    imgURL: string
    
}

const { title, description, imgURL } = Astro.props

---
<div>
    <div>
        <img src={imgURL} alt={title}>
    </div>
    <div>
        <h4>{title}</h4>
        <p>{description}</p>
    </div>
</div>
